spring:
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: quest
  zipkin:
    base-url: http://zipkin:9411/
  datasource:
    driver-class-name: org.postgresql.Driver
    username: admin
    password: {cipher}6e2355f04f1767a378273ae156ced7a34188abf7ac0faf30d9b543a890300d92b82786a6aee394f949d42c2cfcc8c6ea
    url: jdbc:postgresql://organization-db:5432/organization_db
  jpa:
    show-sql: true
    hibernate:
        ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080

management:
  tracing:
    sampling:
      probability: 1.0
  zipkin:
    tracing:
      endpoint: http://zipkin:9411/api/v2/spans
  endpoints:
    web:
      exposure:
        include: "*"

springdoc:
  swagger-ui:
    path : /api/organizations/gui-docs
  api-docs:
    path : /api/organizations/api-docs

eureka:
  client:
    service-url:
      defaultZone: http://service-registry:8761/eureka/

logging:
  level:
    org:
      springframework:
        web: DEBUG
  pattern:
    level: '%5p [${spring.application.name:},%X{traceId:-},%X{spanId:-}]'
